{{ form_start(form) }}
<div class="container">
<div class="row mb-3 d-flex justify-content-around">
{{ form_label(form.name, form.name, { 'label_attr': {'class': 'text-light text-center label_width_product'} }) }}
{{ form_widget(form.name, { 'attr': {'class': 'input_width form-control'} }) }}
</div>
<div class="row mb-3 d-flex justify-content-around">
{{ form_label(form.author, form.author, { 'label_attr': {'class': 'text-light text-center label_width_product'} }) }}
{{ form_widget(form.author, { 'attr': {'class': 'input_width form-control'} }) }}
</div>
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
